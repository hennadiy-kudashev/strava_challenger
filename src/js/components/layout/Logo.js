import React from 'react';
import { Link } from 'react-router';

const Logo = () => (
        <Link to="/dashboard" className="logo">
            StravaChallenger
        </Link>
);

export default Logo;
