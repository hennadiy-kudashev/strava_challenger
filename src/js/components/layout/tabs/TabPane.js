import React, {PropTypes} from 'react';

const TabPane = () => {
    return (
        <div className="tab-pane active">
            here goes text
        </div>
    );
};

TabPane.propTypes = {
};

export default TabPane;

