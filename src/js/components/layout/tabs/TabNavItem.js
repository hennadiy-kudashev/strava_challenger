import React, {PropTypes} from 'react';

const TabNavItem = () => {
    return (
        <li className="active">
            <a href="#">Settings</a>
        </li>
    );
};

TabNavItem.propTypes = {
};

export default TabNavItem;
